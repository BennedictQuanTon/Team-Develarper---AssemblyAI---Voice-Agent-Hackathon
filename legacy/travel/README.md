# Legacy Da Nang Travel Agent

This directory preserves the Da Nang tourism RAG implementation, knowledge base, planning material, and historical evaluation evidence that predate the Lantern restaurant voice waiter.

The active application defaults to `agent_mode=waiter`. Travel mode remains available only for compatibility through `agent_mode=rag` and should not be presented as the primary product without an explicit product decision.
