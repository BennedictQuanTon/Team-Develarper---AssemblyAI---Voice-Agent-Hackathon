# Phase 0: core server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
pyyaml>=6.0.2
httpx>=0.27.0

# Phase 1: local RAG ingest (no vendor API keys)
# DefaultEmbeddingFunction (ONNX) — avoids heavy torch/sentence-transformers install
chromadb>=0.5.23
rank-bm25>=0.2.2

